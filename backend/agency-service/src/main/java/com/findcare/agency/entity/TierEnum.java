package com.findcare.agency.entity;

/**
 * Enum representing the subscription tiers available for an agency account.
 * This is used to standardize and restrict the values for subscription tiers
 * to predefined options: basic, standard, or premium.
 */
public enum TierEnum {
    basic,
    standard,
    premium
}